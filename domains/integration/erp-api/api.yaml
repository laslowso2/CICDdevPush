type: api
version: v4.3.0
data:
  name: ERPIntegrationAPI
  context: /erp-integration
  version: "1.0"
  provider: admin
  tags:
  - integration
  - erp
  - demo
  endpointConfig:
    endpoint_type: http
    production_endpoints:
      url: https://localhost:9090/api/erp
    sandbox_endpoints:
      url: https://localhost:9091/api/erp
    endpoint_security:
      production:
        type: BASIC
        enabled: true
        username: admin
        password: admin123
      sandbox:
        type: BASIC
        enabled: true
        username: admin
        password: admin123
