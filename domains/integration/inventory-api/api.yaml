type: api
version: v4.3.0
data:
  name: InventoryAPI
  context: /inventory
  version: "1.0"
  provider: admin
  tags:
  - integration
  - inventory
  - demo
  endpointConfig:
    endpoint_type: http
    production_endpoints:
      url: https://localhost:9090/api/inventory
    sandbox_endpoints:
      url: https://localhost:9091/api/inventory
    endpoint_security:
      production:
        type: BASIC
        enabled: true
        username: admin
        password: admin123
      sandbox:
        type: BASIC
        enabled: true
        username: admin
        password: admin123
